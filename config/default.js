const config = {
    // 启动端口
    port: 3000,

    // 数据库配置
    database: {
        DATABASE: 'runoob',
        USERNAME: 'root',
        PASSWORD: 'yn666666',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config;
